# Nome do arquivo e árvore de localização: /docker-compose.yml
# Função: Orquestrar infraestrutura redirecionando portas externas para evitar colisões no host.
version: '3.8'

services:
  # O Banco de Dados - Rodando direto na RAM/CPU (Sem travar com NVIDIA)
  database:
    image: postgres:14
    container_name: recicla-db-1
    restart: always
    environment:
      POSTGRES_USER: recicla_user
      POSTGRES_PASSWORD: recicla_password
      POSTGRES_DB: recicla
    ports:
      - "5432:5432"
    volumes:
      - /boot/torre/postgres_data:/var/lib/postgresql/data
    networks:
      - recicla_net

  # A API Isolada e Protegida
  api:
    build: .
    container_name: recicla-api-1
    restart: always
    ports:
      - "3000:3000"
    environment:
      - PORT=3000
      - DATABASE_URL=postgresql://recicla_user:recicla_password@database:5432/recicla
      - SERIAL_PORT=null
    depends_on:
      - database
    networks:
      - recicla_net

  # O Gerenciador Visual do Banco de Dados
  adminer:
    image: adminer:latest
    container_name: recicla-adminer-1
    restart: always
    ports:
      - "8888:8080"
    depends_on:
      - database
    networks:
      - recicla_net

networks:
  recicla_net:
    driver: bridge